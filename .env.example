# Environment Variables for Browser Relay Service

# Port untuk server (default: 3000)
PORT=3000

# API Key untuk autentikasi request dari Railway
# PENTING: Ganti dengan key yang kuat dan simpan juga di Railway
RELAY_API_KEY=your-super-secret-key-change-me

# Optional: Display untuk headless mode
# Jika tidak ada display (VM tanpa GUI), browser akan jalan headless
# DISPLAY=:99
